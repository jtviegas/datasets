name: tickers

on:
  workflow_dispatch:
  schedule:
    - cron: '0 23 7 1,4,7,10 *'

jobs:
  execute_pipeline:
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: '0'
    - name: 'install uv'
      run: curl -LsSf https://astral.sh/uv/install.sh | sh
    - name : 'install hf'
      run: curl -LsSf https://hf.co/cli/install.sh | bash
    - name: 'login to hf'
      run: hf auth login --token ${{ secrets.HF_TOKEN }}
    - name: 'setup python env'
      run: |
        uv venv .venv --python 3.11
    - name: 'install datasets library'
      run: uv pip install --python .venv/bin/python tgedr-datasets
    - name: 'download existent dataset'
      run: |
        mkdir ./tmp && \
        ./helper.sh download_dataset jtviegas/ticker_analysis tickers.parquet tmp
    - name: 'get new tickers'
      run: | 
        [ ! -d "./datalake" ] && mkdir ./datalake
        .venv/bin/run --module tgedr_datasets.ticker.etl \
          --classname TickerEtl \
          --callable run \
          --classparams "{\"configuration\": {\"source\": \"nasdaq100\", \"target_url\": \"./tmp/tickers.parquet\"}}" | tee output.log
        cat output.log
    # - name: 'commit and push plots'
    #   run: |
    #       git config --global user.name "github-actions[bot]"
    #       git config --global user.email "github-actions[bot]@users.noreply.github.com"
    #       git add assets/article_distribution.png assets/articles_per_processing_time.png
    #       if git diff --staged --quiet; then
    #         echo "No changes to commit"
    #       else
    #         git commit . -m "auto: update output data"
    #         git push
    #       fi
    - name: 'upload updated dataset'
      run: |
        ./helper.sh upload_dataset jtviegas/ticker_analysis tmp/tickers.parquet 